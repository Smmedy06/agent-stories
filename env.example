# Google Gemini API Key (Required)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# Database URL (Optional - defaults to SQLite)
# For Railway, you can use the default SQLite or configure a PostgreSQL database
DATABASE_URL=sqlite:///./database/story_scenes.db

# Environment (Optional - defaults to production)
# Set to "development" for local development with auto-reload
ENVIRONMENT=production

# Port (Optional - Railway automatically sets this)
# PORT=8000

